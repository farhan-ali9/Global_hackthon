{
  "name": "hacknation5-project",
  "private": true,
  "scripts": {
    "start": "cd city_wallet && npx expo start",
    "android": "cd city_wallet && npx expo start --android",
    "ios": "cd city_wallet && npx expo start --ios",
    "web": "cd city_wallet && npx expo start --web"
  }
}
