import type { GeneratedOffer, Redemption } from "@/src/types/city-wallet";

// Backend team: replace these stubs with Firestore reads/writes when collections are ready.
export async function saveGeneratedOffer(offer: GeneratedOffer) {
  return offer;
}

export async function saveRedemption(redemption: Redemption) {
  return redemption;
}
