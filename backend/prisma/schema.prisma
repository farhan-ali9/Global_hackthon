generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Merchant {
  id          String @id
  description String
  cityId      String
  latitude    Float
  longitude   Float
  rules       String

  @@index([cityId])
}
